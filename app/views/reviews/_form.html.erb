<%= form_with scope: :review, url: reviews_path do |form| %>
	<%= form.hidden_field :project_id, value: @project.id%>
	<%= form.hidden_field :user_id, value: @user.id%>


	Note: this slider below is a placeholder for rating you want to give in the review (0-5, i.e. 6 possible values). Can we use JS to make this better i.e. not an ugly slider? (like amazon rating system?) <br/>
	<%= form.range_field :rating, :min=>0, :max=>5%> <br />
	<%= form.text_area :text, rows: 8, cols: 50, placeholder: "Write some comments (Optional)"%> <br />

	<%= link_to "Back to Project", project_path(@project.id), class: "btn btn-secondary"%>
	<%= form.submit "Submit Review!", class: "btn btn-success"%>

<% end %>