<!-- # <h2>New review for <#%="#{@user.full_name} regarding #{@project.title}"%></h2> -->
<%= form_with scope: :review, url: reviews_path do |form| %>
  <%= form.hidden_field :user_id, value: user_id, id: "user_id_#{user_id}"%>
  <div>
    <%= form.label :rating%>
  </div>
  <div id='star-<%=user_id%>'></div>
  <%= form.text_area :text, rows: 8, cols: 50, placeholder: "Write some comments (Optional)", id: "review_text_#{user_id}"%> <br />

  <%= link_to "Back to Project", project_path(@project.id), class: "btn btn-secondary"%>
  
  <%= form.button "Submit Review!", type: "submit", name: "review[project_id]", value: @project.id, class: "btn btn-success"%>

  <script>
    $('#star-<%=user_id%>').raty({
        path: '/assets',
        scoreName: 'review[rating]',
    });
  </script>
<% end %>